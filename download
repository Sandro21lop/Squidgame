# Secretos — NUNCA subir esto al repo
.env

# Bases de datos locales (en Render usás Postgres, no esto)
*.db
instance/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Instalador del juego — pesa mucho, mejor no versionarlo en Git
# (si querés que Render lo sirva igual, hay que subirlo aparte, no acá)
downloads/*.exe

# Sistema
.DS_Store
